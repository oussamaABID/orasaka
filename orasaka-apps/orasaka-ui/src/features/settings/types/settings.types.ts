/**
 * @deprecated Import from `@/core/types/settings.types` instead.
 * This barrel re-export exists only for backward compatibility.
 */
export type { Settings } from "@/core/types/settings.types";
