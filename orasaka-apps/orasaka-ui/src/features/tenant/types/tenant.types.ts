/**
 * @deprecated Import from `@/core/types/tenant.types` instead.
 * This barrel re-export exists only for backward compatibility.
 */
export type { TenantConfig } from "@/core/types/tenant.types";
