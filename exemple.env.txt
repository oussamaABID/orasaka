# ==============================================================================
# SYSTEM ENVIRONMENT CONFIGURATION
# ==============================================================================

# 1. NETWORK PORT & CORS SECURITY
PORT=8080
GATEWAY_CORS_ALLOWED_ORIGINS=http://localhost:3000

# 2. UI CONFIGURATION
UI_URL=http://localhost:3000

# 3. DATABASE & CACHE CONNECTIONS & RABBITMQ BROKER CONNECTIONS
SPRING_DATASOURCE_URL=jdbc:postgresql://localhost:5432/orasaka_db
SPRING_DATASOURCE_USERNAME=orasaka_admin
SPRING_DATASOURCE_PASSWORD=orasaka secure pass
SPRING_RABBITMQ_HOST=localhost
SPRING_RABBITMQ_PORT=5672
REDIS_URL=redis://localhost:6379

# 4. CORE DOCKER ALIASES
POSTGRES_CONTAINER=orasaka-postgres
REDIS_CONTAINER=orasaka-redis
RABBITMQ_CONTAINER=orasaka-rabbitmq

# 5. BASE FOLDER ANCHORS
UPLOAD_DIR=var/orasaka-uploads

# 6. AI PROVIDER CORE CONFIGURATIONS
DEFAULT_PROVIDER=ollama
OLLAMA_BASE_URL=http://localhost:11434
OLLAMA_MODEL=llama3.2:3b
OLLAMA_EMBEDDING_MODEL=nomic-embed-text:latest
ROUTER_PROVIDER=ollama
ROUTER_MODEL=llama3.2:3b

# 7. WORKER INFRASTRUCTURE PORTS
VIDEO_WORKER_PORT=8188
IMAGE_WORKER_PORT=8085

# 8. OLLAMA CACHE & CONTEXT RETENTION
OLLAMA_NUM_PARALLEL=1
OLLAMA_KEEP_ALIVE=24h

# 9. CRYPTOGRAPHY (AES-256 for JPA field encryption)
CRYPTO_KEY=orasaka-secret-key-key
CRYPTO_SALT=1234567890abcdef

# 10. SONAR KEY
SONAR_TOKEN=KEY

# 11. TEST CREDENTIALS
ADMIN_EMAIL=admin@orasaka.com
ADMIN_PASSWORD=admin
USER_EMAIL=user@orasaka.com
USER_PASSWORD=user

# 12. TIMEOUT CONFIGURATIONS
ASYNC_TIMEOUT_MS=600000
JOBS_TIMEOUT_SECONDS=600

# 13. LOGGING FILE LOCATIONS (date stamp applied at runtime by start.sh)
LOG_DIR=var/logs
GATEWAY_LOG=var/logs/gateway.log
AUTOMATION_WORKER_LOG=var/logs/automation-worker.log
IMAGE_WORKER_LOG=var/logs/image-worker.log
VIDEO_WORKER_LOG=var/logs/video-worker.log
IMAGE_GEN_WORKER_LOG=var/logs/image-generation-worker.log

# 14. DATE-STAMPED LOG PATHS (resolved dynamically by test scripts)
# Pattern: YYYYMMDD_<service>.log — matches start.sh output redirection
GATEWAY_LOG_PATH=var/logs/orasaka-core.log
VIDEO_WORKER_LOG_PATH=var/logs/video-worker.log

# 15. WORKER SEED PARAMETERS (A-1)
IMAGE_GEN_SEED=-1
VIDEO_GEN_SEED=-1
