# ==============================================================================
# ORASAKA — SYSTEM ENVIRONMENT CONFIGURATION (TEMPLATE)
# ==============================================================================
# Copy this file to .env and fill in your values.
# The CLI wizard `npx orasaka install` can generate this for you automatically.
#
# This file is committed to Git — NEVER put real secrets here.
# ==============================================================================

# ── 1. NETWORK & CORS ────────────────────────────────────────────────────────
PORT=8080
GATEWAY_CORS_ALLOWED_ORIGINS=http://localhost:3000

# ── 2. DATABASE ──────────────────────────────────────────────────────────────
SPRING_DATASOURCE_URL=jdbc:postgresql://localhost:5432/orasaka_db
SPRING_DATASOURCE_USERNAME=orasaka_admin
SPRING_DATASOURCE_PASSWORD=CHANGE_ME

# ── 3. MESSAGE BROKER (RabbitMQ) ─────────────────────────────────────────────
SPRING_RABBITMQ_HOST=localhost
SPRING_RABBITMQ_PORT=5672

# ── 4. CACHE (Redis) ────────────────────────────────────────────────────────
REDIS_URL=redis://localhost:6379

# ── 5. AI PROVIDERS ─────────────────────────────────────────────────────────
DEFAULT_PROVIDER=ollama
OLLAMA_BASE_URL=http://localhost:11434
SPRING_AI_OLLAMA_BASE_URL=http://localhost:11434
OLLAMA_MODEL=llama3.2:3b
OLLAMA_EMBEDDING_MODEL=nomic-embed-text:latest
ROUTER_PROVIDER=ollama
ROUTER_MODEL=llama3.2:3b

# ── 6. WORKERS ───────────────────────────────────────────────────────────────
VIDEO_WORKER_PORT=8188
IMAGE_WORKER_PORT=8085
IMAGE_GEN_SEED=-1
VIDEO_GEN_SEED=-1
VIDEO_WORKER_PYTHON_PATH=

# ── 7. SECURITY & CRYPTOGRAPHY (AES-256 JPA field encryption) ───────────────
CRYPTO_KEY=CHANGE_ME
CRYPTO_SALT=CHANGE_ME

# ── 8. SONARQUBE ─────────────────────────────────────────────────────────────
SONAR_TOKEN=CHANGE_ME

# ── 9. TEST CREDENTIALS ─────────────────────────────────────────────────────
ADMIN_EMAIL=admin@orasaka.com
ADMIN_PASSWORD=admin
USER_EMAIL=user@orasaka.com
USER_PASSWORD=user

# ── 10. E2E TESTING (ADR-040 — Hermetic Maven Profile) ──────────────────────
DB_LOCAL_PORT=5433
DB_LOCAL_NAME=orasaka_e2e
DB_LOCAL_USERNAME=orasaka_e2e
DB_LOCAL_PASSWORD=orasaka_e2e_pass
GATEWAY_TARGET_URL=http://localhost:8080

# ── 11. TIMEOUTS ─────────────────────────────────────────────────────────────
ASYNC_TIMEOUT_MS=600000
JOBS_TIMEOUT_SECONDS=600

# ── 12. LOGGING ──────────────────────────────────────────────────────────────
LOG_DIR=var/logs
GATEWAY_LOG=var/logs/gateway.log

# ── 13. UPLOADS ──────────────────────────────────────────────────────────────
UPLOAD_DIR=var/orasaka-uploads

# ── 14. FRONTEND & AUTH ──────────────────────────────────────────────────────
NEXT_PUBLIC_UI_URL=http://localhost:3000
UI_URL=http://localhost:3000
GATEWAY_URL=http://localhost:8080
NEXTAUTH_SECRET=CHANGE_ME
NEXTAUTH_URL=http://localhost:3000

# ── 15. DOCKER CONTAINERS ───────────────────────────────────────────────────
POSTGRES_CONTAINER=orasaka-postgres
REDIS_CONTAINER=orasaka-redis
RABBITMQ_CONTAINER=orasaka-rabbitmq

# ── 16. OLLAMA TUNING ───────────────────────────────────────────────────────
OLLAMA_NUM_PARALLEL=1
OLLAMA_KEEP_ALIVE=24h

# ── 17. WORKER LOGS ─────────────────────────────────────────────────────────
AUTOMATION_WORKER_LOG=var/logs/automation-worker.log
IMAGE_WORKER_LOG=var/logs/image-worker.log
VIDEO_WORKER_LOG=var/logs/video-worker.log
IMAGE_GEN_WORKER_LOG=var/logs/image-generation-worker.log
