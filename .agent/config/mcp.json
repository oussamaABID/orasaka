{
  "mcpServers": {
    "orasaka-code-intel": {
      "command": "node",
      "args": [
        "./.agent/mcp-server/node_modules/@modelcontextprotocol/server-filesystem/dist/index.js",
        "./"
      ],
      "description": "Filesystem access for Orasaka monorepo — use ripgrep (grep_search) for pattern-based code scanning instead of iterative file walking"
    }
  }
}
